
"""Environment-based settings loader."""
import os
from .base import *

# Optional: Load environment specific overrides if needed,
# but base.py already aggregates the core logic.
# This keeps it simple and predictable.
