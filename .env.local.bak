# Generated by scripts/dev_up.sh
SOMABRAIN_MEMORY_HTTP_ENDPOINT=http://sb_memory_integrity_worker:9696
# Note: inside Docker network prefer the service hostname (sb_memory_integrity_worker) so
# containers talk to each other directly. Use host.docker.internal only when calling from
# the host machine into containers (macOS). If you need host access, set the env var
# externally or use the previous value.
SOMABRAIN_HOST_PORT=9696
REDIS_HOST_PORT=6379
KAFKA_HOST_PORT=9092
PROMETHEUS_HOST_PORT=9090
POSTGRES_HOST_PORT=15432
SOMABRAIN_MEMORY_FAST_ACK=1
