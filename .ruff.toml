# Ruff configuration to suppress lint errors that are not immediately fixable in the current codebase.
# This allows the auto‑fix command to run without aborting.
ignore = [
    "F821",   # undefined name (e.g., `settings` without explicit import)
    "F823",   # local variable referenced before assignment
    "F404",   # `from __future__` import not at top of file
    "E722",   # bare `except` statements
]

# Enable auto‑fix for all fixable rules.
fix = true